openapi: 3.0.3
info:
  title: Topic API with HAL-FORMS
  version: 1.0.0
paths:
  /topics/{topicId}:
    get:
      summary: Get a topic by ID
      parameters:
        - name: topicId
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: A topic resource
          content:
            application/hal+json:
              schema:
                $ref: '#/components/schemas/Topic'
            application/json:
              schema:
                $ref: '#/components/schemas/Topic'
          links:
            self:
              description: Link to this topic
              operationId: getTopic
  /:
    get:
      summary: Entry point with HAL-FORMS template
      responses:
        '200':
          description: API root with discovery information
          content:
            application/hal+json:
              schema:
                $ref: '#/components/schemas/EntryPoint'

components:
  schemas:
    Topic:
      type: object
      properties:
        topicId:
          type: string
        topicText:
          type: string
        _links:
          type: object
          properties:
            self:
              type: object
              properties:
                href:
                  type: string
                  example: "/topics/123"
      required:
        - topicId
        - topicText

    EntryPoint:
      type: object
      properties:
        _links:
          type: object
          properties:
            topics:
              type: object
              properties:
                href:
                  type: string
                  example: "/topics/{topicId}"
                templated: true
        _templates:
          type: object
          properties:
            getTopic:
              type: object
              properties:
                title:
                  type: string
                  example: "Get Topic"
                method:
                  type: string
                  example: "GET"
                contentType:
                  type: string
                  example: "application/json"
                properties:
                  type: array
                  items:
                    type: object
                    properties:
                      name:
                        type: string
                        example: "topicId"
                      required:
                        type: boolean
                        example: true
                target:
                  type: string
                  example: "/topics/{topicId}"
