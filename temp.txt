@echo off
:: Traverse all subdirectories in the current working directory
for /d %%d in (*) do (
    if exist "%%d\.git" (
        echo Fetching updates for %%d
        pushd "%%d"
        git fetch
        popd
    )
)
pause
